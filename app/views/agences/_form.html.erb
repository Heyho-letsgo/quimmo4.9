<%= simple_form_for(@agence) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nom %>
    <%= f.association :principal_groupe %>
    <%= f.association :sous_groupe %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
